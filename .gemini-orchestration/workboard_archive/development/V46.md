# 개발 에이전트 워크보드 [ROUND 18 / REVISION 46]

> Agent: `개발 에이전트` (Development Agent)
> Version: `V1-RESTART-REVISION-46`
> Date: `2026-03-11`
> Status: `작업지시완료` (Manager: Dispatched)

## 💬 매니저 전달용 채팅 지시문 (Manager's Command)
"개발 에이전트님, 데이터 에이전트가 복구 완료한 **196건의 연관 데이터(Related Vocab)**를 UI에 완벽하게 반영하십시오. 
1. **[가시성 보완]**: 상세 패널의 '연관 어휘' 섹션이 데이터가 존재할 경우 학습자가 즉시 인지할 수 있도록 시각적 강조를 보완하십시오.
2. **[점프 로직 검증]**: 연관 어휘 클릭 시 마인드맵이 해당 카테고리를 자동으로 펼치고(`Expand`), 해당 단어로 줌인(`Zoom-to-node`)하는 동기화 로직의 무결성을 최종 점검하십시오. 
데이터가 정상적으로 렌더링되는지 확인 후 보고하십시오."

---

## 1. Current Task: [V1-REV-46] 복구 연관 데이터 UI 반영 및 동기화 무결성 확보

### Goal: 복구된 연관 어휘 데이터를 활용하여 앱의 '의미망 탐색' 경험을 극대화함.

1.  **[UI/UX] 연관 어휘 섹션 가시성 강화**:
    *   `TermDetail.jsx` 내 `related_vocab` 렌더링 시, 데이터가 있을 때 상단에 배지나 아이콘을 활용하여 강조.
    *   연관 어휘 칩(Chip)의 클릭 유도 디자인(Affordance) 개선.
2.  **[동기화] 3대 축 간 '초연결 횡단 점프' 로직 완결**:
    *   **시나리오**: 연관 어휘 클릭 시, 해당 단어가 현재 활성화된 탭(축)이 아닌 타 축(예: 상황 ➔ 마음)에 소속되어 있을 경우의 자동 처리.
    *   **정밀 시퀀스 구현**:
        1.  **탭 자동 전환**: 클릭된 단어의 `system` 정보에 맞춰 상단 탭을 즉시 교체.
        2.  **데이터 동기화 대기**: 새로운 축의 JSON 데이터 로딩이 완료될 때까지 로직 대기(Wait for data).
        3.  **카테고리 자동 확장**: 해당 단어가 속한 중분류(Category)를 찾아 마인드맵에서 강제로 `Expand` 상태로 전환.
        4.  **자동 포커싱 및 줌**: 최종적으로 해당 단어 노드를 화면 중앙에 배치하고 `1.8x` 수준으로 다이나믹하게 확대(Zoom).
    *   **사용자 경험**: 탭이 바뀌더라도 사용자가 선택한 단어를 놓치지 않고 마인드맵이 그 위치로 친절하게 안내해야 함.
3.  **[예외 처리] 데이터 정합성 UI 체크**:
    *   `related_vocab` 배열 내 단어가 실제 검색 인덱스에 존재하지 않을 경우, 클릭 불가 처리 또는 '검색 인덱스 누락' 디버깅 로그 출력.

---

## 2. Latest Report (Previous)
- **2026-03-11**: [V1-REV-43] UI 전면 개편(단계별 렌더링, 정책 반영 등) 완료 (최종 승인 종료).
